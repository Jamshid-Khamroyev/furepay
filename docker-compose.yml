version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://169.58.221.22
        VITE_GOOGLE_CLIENT_ID: 490167420024-1n6tdeh8c7lmcfm381crftnv9vmfchhu.apps.googleusercontent.com
    ports:
      - "80:3001"
    env_file:
      - ./backend/.env
    restart: unless-stopped
